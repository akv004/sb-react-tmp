{
  "name": "angularjs-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "angular": "1.5.11"
  },
  "scripts": {
    "build": "mkdir -p dist/components && cp src/app.js dist && cp src/components/*.js dist/components/"
  }
}
